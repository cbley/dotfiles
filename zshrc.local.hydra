######
# shell config local to hyrdra
######

# update dotfiles repo on login
cd /home/chris/.dotfiles && \
    git pull && \
    git submodule init > /dev/null && \
    git submodule update && \
    cd -
